const groupMock = require('./mocks/groupMock.json')
const pgDB = require('./../src/pg-database')
const pgServices = require('./../src/pg-services')
const pgResponses = require('./../src/pg-services')

describe('Create group', () => {
    
})
